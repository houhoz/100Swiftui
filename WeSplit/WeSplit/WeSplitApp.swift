//
//  WeSplitApp.swift
//  WeSplit
//
//  Created by didi on 2023/12/13.
//

// WeSplitApp.swift 包含用于启动应用程序的代码。如果您在应用程序启动时创建了一些内容，并使其始终保持激活状态，则应将其放在此处。
//Assets.xcassets 是一个资产目录，收集了你想在应用中使用的图片。您还可以在这里添加颜色以及应用图标、iMessage 贴纸等。
//Preview Content（预览内容）是一个组，其中包含 Preview Assets.xcassets--这是另一个资产目录，这次是专门为你在设计用户界面时要使用的示例图片而设，让你了解程序运行时这些图片会是什么样子。

import SwiftUI

@main
struct WeSplitApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
